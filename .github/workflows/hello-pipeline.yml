name: Hello Pipeline 👋 (Multi-Stage)

on:
  workflow_dispatch:
    inputs:
      message:
        description: "Enter a custom message"
        required: true
        default: "Hello from Nouman’s pipeline!"

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "👋 ${{ github.event.inputs.message }}"

  build:
    runs-on: ubuntu-latest
    needs: greet
    steps:
      - name: Simulate Build
        run: echo "🏗️ Building project for: ${{ github.event.inputs.message }}"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Simulate Deploy
        run: echo "🚀 Deploying app for: ${{ github.event.inputs.message }}"
